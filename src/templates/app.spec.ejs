const { mockClient } = require('aws-sdk-client-mock');
const handler = require('../app.js');

describe('Unit Testing', () => {

    test('Happy Flow', () => {
      await handler();
      
    });
  
    test('Failed case', () => {
        await handler();
    });
}) 